{
  "name": "aubiojs",
  "version": "0.0.0",
  "main": "build/aubio.js",
  "module": "build/aubio.esm.js",
  "files": [
    "build/aubio.js",
    "build/aubio.esm.js"
  ],
  "scripts": {
    "build": "export NODE_PATH=$(npm root -g) && emcmake cmake -Bbuild -H. && make -Cbuild -j",
    "test": "jasmine"
  },
  "devDependencies": {
    "jasmine": "^3.9.0",
    "node-fetch": "^3.0.0",
    "wav-decoder": "^1.3.0"
  }
}
