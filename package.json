{
  "name": "aubiojs",
  "version": "0.0.0",
  "main": "build/aubio.js",
  "module": "build/aubio.mjs",
  "files": [
    "build/aubio.js",
    "build/aubio.mjs"
  ],
  "scripts": {
    "build": "export NODE_PATH=$(npm root -g) && emcmake cmake -Bbuild -H. && make -Cbuild -j && esbuild build/aubio.js --minify --format=esm --outfile=build/aubio.mjs && esbuild build/aubio.js --minify --format=cjs --allow-overwrite --outfile=build/aubio.js",
    "test": "jasmine"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.6",
    "esbuild": "^0.15.13",
    "jasmine": "^3.9.0",
    "node-fetch": "^3.0.0",
    "wav-decoder": "^1.3.0"
  }
}
